# 家庭 Web 入口预留

状态：结构预留。

当前没有独立的 Family OS Web 应用；此处无可运行入口。

后续 src/app、components、features、hooks、services、store、styles 按实际技术选型创建；不复制 Website PR 中的代码。

参见 [迁移计划](../../docs/roadmap/structure-migration.md)。
