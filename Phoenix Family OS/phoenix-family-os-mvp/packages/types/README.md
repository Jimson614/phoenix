# types

状态：结构预留，无独立 package.json 或可导入模块。

职责：经确认的跨模块类型与版本契约。

当前来源：

- [models/schema.js](../../models/schema.js)
- [models/partner-experience.d.ts](../../models/partner-experience.d.ts)

业务 schema 与身份契约须先对齐；本次不发明新的 ID、字段或类型包。
